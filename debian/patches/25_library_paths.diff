Author: LaMont Jones <lamont@debian.org>
Date:   Thu Nov 8 15:16:39 2007 -0700

    Makefile.in: be explicit about library paths

    Debian policy requires that all dependant libs be in the .so, not just the
    immediately depended ones.

    Signed-off-by: LaMont Jones <lamont@debian.org>

--- bind9.orig/lib/dns/Makefile.in
+++ bind9/lib/dns/Makefile.in
@@ -43,7 +43,7 @@ ISCLIBS =	../../lib/isc/libisc.@A@
 
 ISCDEPLIBS =	../../lib/isc/libisc.@A@
 
-LIBS =		@LIBS@
+LIBS =		@LIBS@ -L../../lib/isc -lcrypto
 
 # Alphabetically
 
@@ -146,6 +146,7 @@ libdns.la: ${OBJS}
 		${CC} ${ALL_CFLAGS} ${LDFLAGS} -o libdns.la -rpath ${libdir} \
 		-version-info ${LIBINTERFACE}:${LIBREVISION}:${LIBAGE} \
 		${OBJS} ${ISCLIBS} @DNS_CRYPTO_LIBS@ ${LIBS}
+	ln -sf .libs/libdns.so .
 
 include: gen
 	${MAKE} include/dns/enumtype.h
@@ -192,6 +193,7 @@ clean distclean::
 	rm -f libdns.@A@ timestamp
 	rm -f gen code.h include/dns/enumtype.h include/dns/enumclass.h
 	rm -f include/dns/rdatastruct.h
+	rm -f libdns.so
 
 newrr::
 	rm -f code.h include/dns/enumtype.h include/dns/enumclass.h
--- bind9.orig/lib/isc/Makefile.in
+++ bind9/lib/isc/Makefile.in
@@ -123,12 +123,14 @@ libisc.la: ${OBJS} ${SYMTBLOBJS}
 		${CC} ${ALL_CFLAGS} ${LDFLAGS} -o libisc.la -rpath ${libdir} \
 		-version-info ${LIBINTERFACE}:${LIBREVISION}:${LIBAGE} \
 		${OBJS} ${SYMTBLOBJS} ${LIBS}
+	ln -sf .libs/libisc.so .
 
 libisc-nosymtbl.la: ${OBJS}
 	${LIBTOOL_MODE_LINK} \
 		${CC} ${ALL_CFLAGS} ${LDFLAGS} -o libisc-nosymtbl.la -rpath ${libdir} \
 		-version-info ${LIBINTERFACE}:${LIBREVISION}:${LIBAGE} \
 		${OBJS} ${LIBS}
+	ln -sf .libs/libisc-nosymtbl.so .
 
 timestamp: libisc.@A@ libisc-nosymtbl.@A@
 	touch timestamp
@@ -147,3 +149,4 @@ uninstall::
 clean distclean::
 	rm -f libisc.@A@ libisc-nosymtbl.@A@ libisc.la \
 	libisc-nosymtbl.la timestamp
+	rm -f libisc.so libisc-nosymtbl.so
--- bind9.orig/lib/isccc/Makefile.in
+++ bind9/lib/isccc/Makefile.in
@@ -37,7 +37,7 @@ ISCCCLIBS =	../../lib/isccc/libisccc.@A@
 ISCDEPLIBS =	../../lib/isc/libisc.@A@
 ISCCCDEPLIBS =	libisccc.@A@
 
-LIBS =		@LIBS@
+LIBS =		@LIBS@ -L../../lib/isc
 
 SUBDIRS =	include
 
@@ -73,6 +73,7 @@ libisccc.la: ${OBJS}
 		${CC} ${ALL_CFLAGS} ${LDFLAGS} -o libisccc.la -rpath ${libdir} \
 		-version-info ${LIBINTERFACE}:${LIBREVISION}:${LIBAGE} \
 		${OBJS} ${ISCLIBS} ${LIBS}
+	ln -sf .libs/libisccc.so .
 
 timestamp: libisccc.@A@
 	touch timestamp
@@ -88,3 +89,4 @@ uninstall::
 
 clean distclean::
 	rm -f libisccc.@A@ timestamp
+	rm -f libisccc.so
--- bind9.orig/lib/isccfg/Makefile.in
+++ bind9/lib/isccfg/Makefile.in
@@ -37,7 +37,7 @@ ISCCFGLIBS =	../../lib/cfg/libisccfg.@A@
 ISCDEPLIBS =	../../lib/isc/libisc.@A@
 ISCCFGDEPLIBS =	libisccfg.@A@
 
-LIBS =		@LIBS@
+LIBS =		@LIBS@ -L../dns -L../isc -L../isccc
 
 SUBDIRS =	include
 
--- bind9.orig/lib/irs/Makefile.in
+++ bind9/lib/irs/Makefile.in
@@ -43,7 +43,7 @@ SRCS =		context.c \
 		gai_strerror.c getaddrinfo.c getnameinfo.c \
 		resconf.c
 
-LIBS =		@LIBS@
+LIBS =		@LIBS@ -L../../lib/isc -L../../lib/dns -L../../lib/isccfg -lcrypto -lisc -ldns -lisccfg
 
 SUBDIRS =	include
 TESTDIRS =	@UNITTESTS@
@@ -68,6 +68,7 @@ libirs.la: ${OBJS} version.@O@
 		${CC} ${ALL_CFLAGS} ${LDFLAGS} -o libirs.la -rpath ${libdir} \
 		-version-info ${LIBINTERFACE}:${LIBREVISION}:${LIBAGE} \
 		${OBJS} version.@O@ ${LIBS}
+	ln -sf .libs/libirs.so .
 
 timestamp: libirs.@A@
 	touch timestamp
@@ -85,3 +86,4 @@ uninstall::
 
 clean distclean::
 	rm -f libirs.@A@ libirs.la timestamp
+	rm -f libdns.so
